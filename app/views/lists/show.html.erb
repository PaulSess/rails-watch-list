<h1><%= @list.name %></h1>

<% @movies.each do |movie| %>
  <%= movie.title %>
  <%= movie.poster_url %>
<% end %>

  <%= link_to "+Add movies to the list",lists_path %>
  <%= link_to "go back",lists_path %>
